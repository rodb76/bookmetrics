% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_character_mentions.R
\name{extract_character_mentions}
\alias{extract_character_mentions}
\title{Extract character mentions from text}
\usage{
extract_character_mentions(input_data, character_names)
}
\arguments{
\item{input_data}{A tibble containing \code{chapter} (integer) and \code{text} (character) columns.}

\item{character_names}{A character vector of names to search for.}
}
\value{
A tibble containing \code{chapter} (integer), \code{character} (character),
and \code{mentions} (integer). Only returns rows where \code{mentions > 0}.
}
\description{
Identifies and counts occurrences of specific character names within
chapter text using word boundaries to ensure exact matches.
}
\examples{
\dontrun{
library(tibble)
library(dplyr)
library(stringr)
library(tidyr)

chapters <- tibble(
  chapter = c(1L, 1L),
  text = c("Alice met Bob.", "Bob went home.")
)
characters <- c("Alice", "Bob", "Charlie")

extract_character_mentions(chapters, characters)
}
}
