% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_key_events.R
\name{identify_key_events}
\alias{identify_key_events}
\title{Identify key narrative events}
\usage{
identify_key_events(data)
}
\arguments{
\item{data}{A tibble containing \code{chapter} (integer), \code{momentum} (numeric),
and \code{intensity} (numeric).}
}
\value{
A tibble containing \code{chapter} (integer), \code{event_type} (character),
and \code{metric_value} (numeric) for each identified event.
}
\description{
Detects significant narrative milestones such as breakthroughs,
collapses, and turning points based on momentum and intensity.
}
\examples{
\dontrun{
library(tibble)
data <- tibble(
  chapter = 1:5L,
  momentum = c(0, 0.2, -0.1, 0.4, -0.3),
  intensity = c(0, 0.2, 0.1, 0.4, 0.3)
)
identify_key_events(data)
}
}
